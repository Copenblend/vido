<UserControl x:Class="Vido.Setup.Pages.OptionsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <!-- Checkbox style matching dark theme -->
        <Style x:Key="InstallerCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Margin" Value="0,6" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="40,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Installation Options"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource PrimaryForegroundBrush}"
                   Margin="0,0,0,20" />

        <!-- Options -->
        <StackPanel Grid.Row="1">
            <CheckBox Style="{StaticResource InstallerCheckBox}"
                      IsChecked="{Binding Options.CreateDesktopShortcut}"
                      Content="Create Desktop shortcut" />

            <CheckBox Style="{StaticResource InstallerCheckBox}"
                      IsChecked="{Binding Options.CreateStartMenuShortcut}"
                      Content="Create Start Menu shortcut" />

            <CheckBox Style="{StaticResource InstallerCheckBox}"
                      IsChecked="{Binding Options.RegisterFileAssociations}"
                      Content="Register file associations (.mp4, .avi, .mkv, .mov, .wmv, .flv, .webm)" />
        </StackPanel>

        <!-- Install location -->
        <StackPanel Grid.Row="2" VerticalAlignment="Top" Margin="0,20,0,0">
            <TextBlock Text="Install location:"
                       FontSize="12"
                       Foreground="{StaticResource SecondaryForegroundBrush}"
                       Margin="0,0,0,4" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0"
                        Background="{StaticResource InputBackgroundBrush}"
                        CornerRadius="3">
                    <TextBox x:Name="InstallPathTextBox"
                             Text="{Binding Options.InstallPath, UpdateSourceTrigger=PropertyChanged}"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{StaticResource PrimaryForegroundBrush}"
                             FontSize="12"
                             Padding="8,6"
                             VerticalContentAlignment="Center" />
                </Border>
                <Button Grid.Column="1"
                        Content="Browse..."
                        Click="BrowseButton_Click"
                        Margin="8,0,0,0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="BrowseBorder"
                                                Background="{StaticResource ButtonDefaultBackgroundBrush}"
                                                CornerRadius="3"
                                                Padding="12,6">
                                            <TextBlock Text="Browse..."
                                                       FontSize="12"
                                                       Foreground="White"
                                                       VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="BrowseBorder"
                                                        Property="Background"
                                                        Value="{StaticResource ButtonBackgroundBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </StackPanel>

        <!-- Install button -->
        <Button Grid.Row="3"
                Command="{Binding InstallCommand}"
                Width="200" Height="40"
                HorizontalAlignment="Center"
                Margin="0,0,0,12"
                Cursor="Hand">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="ButtonBorder"
                                        Background="{StaticResource ButtonDefaultBackgroundBrush}"
                                        CornerRadius="4">
                                    <TextBlock Text="Install"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="White"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ButtonBorder"
                                                Property="Background"
                                                Value="{StaticResource ButtonBackgroundBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>

        <!-- Back link -->
        <TextBlock Grid.Row="4"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20">
            <Hyperlink Command="{Binding GoBackCommand}"
                       Foreground="{StaticResource SecondaryForegroundBrush}"
                       TextDecorations="None">
                Back
            </Hyperlink>
        </TextBlock>
    </Grid>
</UserControl>
