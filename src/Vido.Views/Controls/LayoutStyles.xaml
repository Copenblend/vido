<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Themes/Brushes.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Activity bar icon button style (48x48 hit area, centered icon) -->
    <Style x:Key="ActivityBarButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="48" />
        <Setter Property="Height" Value="48" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- Active indicator (left border, 2px accent blue) -->
                        <Border x:Name="ActiveIndicator"
                                HorizontalAlignment="Left"
                                Width="2"
                                Background="{DynamicResource AccentBrush}"
                                Visibility="Collapsed" />

                        <!-- Icon content area (no background highlight — icons light up on hover) -->
                        <Border Background="Transparent"
                                Padding="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Active">
                            <Setter TargetName="ActiveIndicator" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Sidebar header text style -->
    <Style x:Key="SidebarHeaderStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="20,8,0,8" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- GridSplitter style for panel resizing -->
    <Style x:Key="VerticalSplitterStyle" TargetType="GridSplitter">
        <Setter Property="Width" Value="1" />
        <Setter Property="Background" Value="{DynamicResource PrimaryBorderBrush}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Cursor" Value="SizeWE" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Grid Margin="-5,0">
                        <!-- Wider transparent hit area for easier grabbing -->
                        <Border Background="Transparent"
                                Width="11"
                                HorizontalAlignment="Center" />
                        <!-- Visible 1px divider line (rendered on top) -->
                        <Border Background="{DynamicResource PrimaryBorderBrush}"
                                Width="1"
                                HorizontalAlignment="Center"
                                IsHitTestVisible="False" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
