<UserControl x:Class="Vido.Views.Controls.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource SidebarBackgroundBrush}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="LayoutStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <DockPanel>
        <!-- Sidebar header -->
        <TextBlock DockPanel.Dock="Top"
                   Text="{Binding HeaderText}"
                   Style="{StaticResource SidebarHeaderStyle}" />

        <!-- Panel content area — populated by MainWindow based on active panel -->
        <ContentPresenter x:Name="PanelHost" />
    </DockPanel>
</UserControl>
